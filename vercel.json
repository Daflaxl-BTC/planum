{
  "installCommand": "cd landing-page && npm install && cd ../app && npm install",
  "buildCommand": "cd landing-page && npm run build && cd ../app && npm run build && cp -r dist ../landing-page/dist/app",
  "outputDirectory": "landing-page/dist",
  "framework": null,
  "rewrites": [
    { "source": "/app/:path*", "destination": "/app/index.html" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
